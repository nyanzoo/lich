apiVersion: v1
kind: Service
metadata:
  name: operator
spec:
  clusterIP: None
  selector:
    app: operator
  ports:
    - name: operator
      protocol: TCP
      port: 6666
      targetPort: 6666
  internalTrafficPolicy: Cluster
