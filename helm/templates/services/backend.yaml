apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  clusterIP: None
  selector:
    app: backend
  ports:
    - name: backend
      protocol: TCP
      port: 6666
      targetPort: 6666
  internalTrafficPolicy: Cluster
