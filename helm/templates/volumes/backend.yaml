apiVersion: v1
kind: PersistentVolume
metadata:
  name: backend
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /opt/lich
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backend-pvc
spec:
  storageClassName: ""
  volumeName: backend
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
